<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- READ_EXTERNAL_STORAGE is still requested on API <= 32 to keep directory-mode
         (recursive exiftool over a folder) working on older devices. On API 33+ we
         rely entirely on the SAF picker + per-URI grants. WRITE_EXTERNAL_STORAGE is
         no longer needed: file writes go through ContentResolver.openFileDescriptor. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- ACCESS_MEDIA_LOCATION lets the OS hand back unredacted EXIF (in particular
         GPSLatitude/GPSLongitude) on Android 10+. Without it, scoped-storage
         redaction can strip location tags from the bytes we read via SAF on some
         devices (notably Samsung), so exiftool sees a file that genuinely lacks
         the GPS block. Runtime grant is requested in MainActivity. See issue #8. -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
